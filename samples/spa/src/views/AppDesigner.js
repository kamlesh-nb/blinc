import { div } from "../../../../src/types"

const AppDesigner = (props) => {
    const render = () => {
      return div(["AppDesigner"]);
    };
    return { render };
};

export default AppDesigner