import { div } from "../../../../src/types"

const ServiceDesigner = (props) => {
    const render = () => {
      return div(["ServiceDesigner"]);
    };
    return { render };
};

export default ServiceDesigner